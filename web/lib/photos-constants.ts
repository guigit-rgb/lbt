// Fichier à part (pas dans lib/actions/annonces.ts) car un fichier "use
// server" ne peut exporter que des fonctions async — pas de constantes.
export const MAX_PHOTOS = 12;
export const MAX_TAILLE_PHOTO_OCTETS = 8 * 1024 * 1024;
